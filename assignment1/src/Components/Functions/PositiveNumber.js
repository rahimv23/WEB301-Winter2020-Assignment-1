/*
import React from 'react';
import count from '../Number/Number';


const positiveNumber = (props) => {
          
          if (count >= 0) {
            return (
            <CaretButton handleClick={() => {
              
              return setCount(count - 1);
            }}>
              <i class="material-icons">
                keyboard_arrow_down
              </i>
            </CaretButton>
            )
          }
          }

export default positiveNumber;        
*/
